export type MeProps = {
  user: {
    _id: string;
    firstName: string;
    lastName: string;
    username: string;
    email: string;
    isAdmin: boolean;
    confirmed: boolean;
    createdAt: string;
  };
};
