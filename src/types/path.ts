export enum Path {
  HOME = '/',
  REGISTER = '/register',
  LOGIN = '/login',
  CONFIRM = '/confirm/:id',
  EXPLORE = '/explore',
  PROFILE = '/:username/:page?',
  SETTINGS = '/settings/:page?',
  TEST = '/test',
}
