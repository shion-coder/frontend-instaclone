import React, { FC } from 'react';

import { Container } from './saved.styles';

/* -------------------------------------------------------------------------- */

const Saved: FC = () => <Container>Saved</Container>;

export default Saved;
