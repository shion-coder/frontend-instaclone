import styled from 'styled-components';

/* -------------------------------------------------------------------------- */

export const Label = styled.label`
  position: relative;
`;

export const Input = styled.input`
  display: none;
`;
