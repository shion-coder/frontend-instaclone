import React, { FC } from 'react';

import { Container } from './suggestion.styles';

/* -------------------------------------------------------------------------- */

const Suggestion: FC = () => <Container>{}</Container>;

export default Suggestion;
